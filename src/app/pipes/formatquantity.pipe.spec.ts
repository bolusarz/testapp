import { FormatquantityPipe } from './formatquantity.pipe';

describe('FormatquantityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatquantityPipe();
    expect(pipe).toBeTruthy();
  });
});
